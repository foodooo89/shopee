# ShopeeProxy

Build:
1. cd app/src/main/jni && git clone --recursive https://github.com/heiher/hev-socks5-tunnel
2. Android Studio: mở thư mục này, SDK Manager cài NDK (Side by side) + CMake không cần.
3. Build > Build APK(s)  (hoặc: gradle assembleRelease  -> app/build/outputs/apk/release)
